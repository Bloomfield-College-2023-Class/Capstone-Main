const { Box } = require("@mui/material");
const { styled } = require("@mui/system");

// styled components are custom MUI components 
const SpaceBetween = styled(Box)({
  marginBottom: "1rem"
})


export default SpaceBetween;